defaults:
  - _self_

data:
  path: "./data/raw"
  channels: [647, 771]
  sample_rate: 1000.0
  
model:
  learning_rate: 0.001
  batch_size: 16
  
training:
  epochs: 100
  gpus: 1
  
logging:
  mlflow:
    experiment_name: "das_cleaning"
    tracking_uri: "http://localhost:5000"
